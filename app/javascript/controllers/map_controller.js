import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="map"
export default class extends Controller {
  connect() {
  }
}
// on a juste ajouté cette commande, la suite n'est pas faite rails generate stimulus map
